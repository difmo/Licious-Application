/// Placeholder auth controller for future business logic.
class AuthController {
  // void login(String email, String password) { ... }
  // void register(String email, String phone, String password) { ... }
  // void logout() { ... }
}
